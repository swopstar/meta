# react-ui: Roadmap

## Done

- Core component set (Button, Input, Card, Dialog, Dropdown, Select, Tabs, etc.)
- Theme system with `ThemeProvider` and palette generation
- Bundled fonts — Nunito Sans, Geist Mono
- Storybook with accessibility addon
- Dual ESM + CJS build, TypeScript declarations

## Upcoming

- Build out component set via swoptape frontend
- Backport to swopcart once swoptape proves the patterns
